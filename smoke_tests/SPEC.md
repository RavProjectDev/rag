# Smoke Test Spec — moved

The canonical acceptance criteria now live in:

**[`specs/current/app-requirements.md`](../specs/current/app-requirements.md)**

That document is the source of truth used by both humans and AI agents to evaluate
whether the API is behaving correctly after any change.

This file is kept here so tools and comments that reference `smoke_tests/SPEC.md`
continue to find a meaningful entry point.
